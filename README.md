# AiCourse

[![Kotlin](https://img.shields.io/badge/Kotlin-1.9+-7F52FF.svg?logo=kotlin)](https://kotlinlang.org)
[![Android](https://img.shields.io/badge/Android-24%2B-3DDC84.svg?logo=android)](https://developer.android.com)
[![Jetpack Compose](https://img.shields.io/badge/Compose-UI-4285F4.svg?logo=jetpack-compose)](https://developer.android.com/jetpack/compose)
[![Ktor](https://img.shields.io/badge/Ktor-Server-087CFA.svg?logo=ktor)](https://ktor.io)
[![Docker](https://img.shields.io/badge/Docker-Ready-2496ED.svg?logo=docker)](https://www.docker.com)
[![Status](https://img.shields.io/badge/Status-In%20Development-yellow.svg)]()

> Экспериментальный полигон для работы с большими языковыми моделями (LLM), интеграции RAG, MCP и создания расширяемых AI-приложений

## 📋 О проекте

**AiCourse** — это многомодульное Android приложение с backend-инфраструктурой, созданное как исследовательская платформа для экспериментов с различными подходами к настройке и расширению функциональности больших языковых моделей.

### Ключевые возможности

- 💬 **Чат с различными LLM**: Поддержка GigaChat (Сбер) и HuggingFace (DeepSeek R1)
- 🔍 **RAG (Retrieval-Augmented Generation)**: 4 режима поиска с векторными эмбеддингами и AI-переранжированием
- 🔧 **MCP (Model Context Protocol)**: Интеграция внешних инструментов через стандартизированный протокол
- 🛠️ **Система инструментов**: Расширяемый фреймворк для добавления новых возможностей
- 📊 **Управление контекстом**: Автоматическая суммаризация истории при превышении лимита токенов
- 🎯 **Гибкая конфигурация**: Настройка промптов, моделей, инструментов для каждого чата

### Целевая аудитория

- Разработчики Android приложений с AI-функциональностью
- Исследователи в области LLM и RAG-систем
- Энтузиасты, экспериментирующие с MCP протоколом

---

## 🚀 Технологический стек

### Android приложение

- **Язык**: Kotlin
- **UI**: Jetpack Compose
- **DI**: Koin
- **База данных**: Room (SQLite)
- **Сеть**: Ktor Client
- **Архитектура**: Clean Architecture + MVI (Model-View-Intent)

### Backend сервисы

- **Язык**: Kotlin/JVM
- **Фреймворк**: Ktor Server
- **MCP**: MCP Kotlin SDK
- **Деплой**: Docker + Docker Compose

### AI & RAG

- **Эмбеддинги**: Ollama (nomic-embed-text)
- **Переранжирование**: Ollama (llama3.2)
- **LLM провайдеры**: GigaChat API, HuggingFace Router
- **Поиск**: Векторный поиск с косинусным сходством

### Дополнительные инструменты

- **Сборка**: Gradle 8.x
- **Push-уведомления**: Firebase Cloud Messaging
- **Telegram**: Telegram Bot API

---

## 🏗️ Архитектура проекта

### Модули Android приложения

| Модуль | Назначение | Ключевые компоненты |
|--------|-----------|---------------------|
| **`app`** | Основное приложение | UI (Compose), ViewModels (MVI), DI конфигурация, Clean Architecture |
| **`core`** | Базовые классы | BaseViewModel для MVI паттерна, общие утилиты |
| **`coretools`** | Фреймворк инструментов | Tool interface, ToolRegistry, ToolExecutor с interceptor chain |
| **`mcpclient`** | MCP клиент | Подключение к MCP серверам через SSE, обнаружение и выполнение инструментов |
| **`prompt`** | Система промптов | SystemPrompt<R> с type-safe парсингом, динамическое переключение промптов |
| **`rag`** | RAG пайплайн | Индексация документов, векторный поиск, reranking, 4 режима работы |

#### Детальное описание модулей

<details>
<summary><b>📱 app</b> — Основное приложение</summary>

**Архитектура:** Clean Architecture (3 слоя) + MVI

**Presentation Layer:**
- `ChatScreen`, `SettingsScreen` — UI на Jetpack Compose
- `ChatViewModel`, `SettingsViewModel` — MVI паттерн
- Message рендеры для различных типов ответов (plain text, JSON, PC build, token stats)

**Domain Layer:**
- Use cases: `SendMessageChatUseCase`, `ClearHistoryChatUseCase`, `SetRagModelUseCase`
- Repository интерфейсы: `ChatRepository`
- `ChatStrategy` — паттерн стратегии для гибкой работы с чатом
- Tools: `ContextWindowManager`, `TokenCompareManager`, `ModelInfoManager`

**Data Layer:**
- Room database: `ChatDatabase`, `ChatDao`, entities
- Remote data sources: `GigaChatDataSource`, `HuggingFaceDataSource`
- `ChatRepositoryImpl` — реализация репозитория
</details>

<details>
<summary><b>🔧 coretools</b> — Фреймворк инструментов</summary>

**Назначение:** Создание расширяемой системы инструментов

**Компоненты:**
- `Tool` interface — базовая абстракция инструмента (name, description, parameters, execute)
- `ToolRegistry` — центральный реестр для регистрации инструментов
- `ToolExecutor` — выполнение инструментов с поддержкой interceptor chain
- `ToolInterceptor` — middleware для логирования, валидации, метрик

**Использование:** Общий фреймворк для `rag` модуля и MCP клиентов
</details>

<details>
<summary><b>📡 mcpclient</b> — MCP клиент</summary>

**Назначение:** Подключение к удаленным MCP серверам

**Компоненты:**
- `McpClient` interface — контракт для MCP клиентов
- `RemoteMcpClientService` — реализация через SSE (Server-Sent Events)
- `McpClientConfig` — конфигурация URL серверов
- Обнаружение доступных инструментов через `listTools()`
- Выполнение инструментов через `callTool()`
</details>

<details>
<summary><b>💬 prompt</b> — Система промптов</summary>

**Назначение:** Type-safe управление системными промптами

**Компоненты:**
- `SystemPrompt<R>` — базовый интерфейс с generic типом ответа
- Реализации:
  - `PlainTextPrompt` → `PlainTextResponse`
  - `RagAssistantPrompt` → `RagAssistantResponse`
  - `JsonOutputPrompt` → JSON парсинг
  - `BuildComputerAssistantPrompt` → структурированные рекомендации
  - `DynamicTemperaturePrompt`, `DynamicModelPrompt`
- `matches(message)` — активация промпта по триггерам
- `parseResponse(rawString)` — type-safe парсинг

**Паттерн:** Полиморфная диспетчеризация на основе содержимого сообщения
</details>

<details>
<summary><b>🔍 rag</b> — RAG пайплайн</summary>

**Назначение:** Retrieval-Augmented Generation для LLM

**Компоненты:**
- `RagPipeline` — основной оркестратор
  - **Ingestion**: TextSplitter → EmbeddingModel → RagRepository (файловое хранилище)
  - **Retrieval**: VectorSearchEngine → Reranker → Top-K результаты
- `VectorSearchEngine` — семантический поиск по косинусному сходству
- `EmbeddingModel` (OllamaEmbeddingService) — генерация эмбеддингов через Ollama
- `Reranker` (OllamaRerankerService) — переранжирование кросс-энкодером

**4 режима работы:**
- `DISABLED` — без RAG
- `STANDARD` — векторный поиск
- `WITH_RERANKER` — поиск + AI переранжирование
- `WITH_MULTIQUERY` — multi-query expansion + переранжирование
</details>

### Backend сервисы

```
┌─────────────────────────────────────────────────────┐
│         Android App (MVI + Clean Architecture)      │
│  Modules: app, core, coretools, mcpclient, rag      │
└─────────────────────────────────────────────────────┘
                        ↓ SSE (Server-Sent Events)
┌─────────────────────────────────────────────────────┐
│              MCP Servers (Backend)                  │
│  ├─ AiCourseBackend (Port 8080)                     │
│  │   Tools: AnimeTool, NotesTool, AdbTool           │
│  │                                                   │
│  └─ NotificationBackend (Port 8081)                 │
│      Tool: send_telegram_message                    │
└─────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────┐
│                 External APIs                       │
│  ├─ GigaChat (Сбер)          — LLM                  │
│  ├─ HuggingFace Router       — LLM (DeepSeek R1)    │
│  ├─ Ollama (localhost:11434) — Embeddings, Rerank   │
│  ├─ Jikan API                — Anime search         │
│  └─ Telegram Bot API         — Notifications        │
└─────────────────────────────────────────────────────┘
```

#### AiCourseBackend (порт 8080)

**Технологии:** Ktor Server, MCP Kotlin SDK, Docker (Alpine + JRE 17)

**MCP Инструменты:**
- AnimeTool (поиск аниме)
- NotesTool (управление задачами)
- AdbTool (управление Android устройствами)

**Endpoints:**
- `GET /sse` — SSE подключение для MCP протокола
- `POST /messages` — Обработка MCP сообщений
- `POST /register-push-token` — Регистрация FCM токена

#### AiCourseNotificationBackend (порт 8081)

**Технологии:** Ktor Server, Telegram Bot API

**MCP Инструмент:**
- `send_telegram_message` — Отправка сообщений в Telegram

---

## 📦 Установка и запуск

### Требования

- **JDK 17+**
- **Android Studio** (последняя стабильная версия)
- **Docker** и **Docker Compose**
- **Ollama** (для RAG функционала)
- **Gradle 8.x** (встроен в проект через Gradle Wrapper)
- **Node.js** (опционально, для mcp-bridge)

### 1. Клонирование репозитория

```bash
git clone https://github.com/your-username/AiCourse.git
cd AiCourse
```

### 2. Настройка переменных окружения

#### Создать файл `.env` в корне проекта

```env
# GigaChat API (Сбер)
GIGACHAT_AUTH_KEY=your_gigachat_api_key_here

# Telegram Bot (для уведомлений)
TELEGRAM_BOT_TOKEN=your_telegram_bot_token
TELEGRAM_CHAT_ID=your_telegram_chat_id
```

#### Создать файл `local.properties` в корне проекта

```properties
# API ключи для Android приложения
GIGACHAT_AUTH_KEY=your_gigachat_api_key_here
HUGGING_FACE_AUTH_KEY=your_huggingface_api_key_here
```

#### Firebase (опционально, для push-уведомлений)

- Скачать `firebase-service.json` из Firebase Console
- Поместить в `AiCourseBackend/`

### 3. Запуск backend сервисов через Docker

```bash
# Собрать и запустить все MCP серверы
docker-compose up -d

# Проверить статус контейнеров
docker-compose ps

# Просмотр логов
docker-compose logs -f

# Остановить сервисы
docker-compose down
```

**После запуска сервисы доступны на:**
- **AiCourseBackend**: http://localhost:8080
- **NotificationBackend**: http://localhost:8081

### 4. Запуск Ollama для RAG

#### Вариант 1: Использовать готовый скрипт

```powershell
cd scripts
.\start_ollama.ps1
```

#### Вариант 2: Вручную

```bash
# Скачать модели (только при первом запуске)
ollama pull nomic-embed-text:latest  # Для эмбеддингов
ollama pull llama3.2:latest          # Для переранжирования

# Настроить переменные окружения и запустить сервер
$env:OLLAMA_MODELS="D:\Programms\Ollama\Models"  # Путь к моделям
$env:OLLAMA_HOST="0.0.0.0"                       # Слушать на всех интерфейсах
$env:OLLAMA_ORIGINS="*"                          # CORS: разрешить все источники

ollama serve
```

**Ollama будет доступна на:** http://localhost:11434

### 5. Запуск Android приложения

#### Через Android Studio

1. Открыть проект в Android Studio
2. Дождаться завершения синхронизации Gradle
3. Выбрать Build Variant:
   - `devDebug` — для разработки (локальные MCP серверы)
   - `prodDebug` — для продакшена (удаленные серверы)
4. Запустить на устройстве или эмуляторе (Run → Run 'app')

#### Через командную строку

```bash
# Сборка APK
.\gradlew assembleDevDebug

# Установка на подключенное устройство
.\gradlew installDevDebug

# Запуск тестов
.\gradlew test

# Очистка проекта
.\gradlew clean
```

### 6. Настройка mcp-bridge (опционально)

Для интеграции локальных MCP серверов с Claude/Cursor IDE:

```bash
cd mcp-bridge
npm install
```

**Отредактировать `bridge.js`:**
- Найти строку `BASE_HOST = 'http://127.0.0.1:8080'`
- Изменить на URL вашего MCP сервера
- Запустить: `node bridge.js`

---

## 🎯 Функционал

### 💬 Чат с LLM

#### Поддерживаемые модели

**GigaChat (Сбер):**
- **GigaChat** (Fast) — базовая модель
- **GigaChat-Pro** — расширенный контекст
- **GigaChat-Max** — максимальная производительность

**HuggingFace:**
- **DeepSeek R1** (через HuggingFace Router)

#### Возможности чата

- ✅ Сохранение истории сообщений в локальной базе данных (Room)
- ✅ Переключение между моделями и API провайдерами
- ✅ Управление историей:
  - **PLAIN** — отправка всей истории
  - **ONE_MESSAGE** — отправка только текущего сообщения
  - **SUMMARIZE** — автоматическая суммаризация старых сообщений
- ✅ Отображение использования токенов (prompt + completion)
- ✅ Информация о модели, обработавшей запрос
- ✅ Function calling (только для GigaChat) — LLM может вызывать MCP инструменты

### 🔍 RAG (Retrieval-Augmented Generation)

#### 4 режима поиска

| Режим | Название | Описание | Производительность |
|-------|----------|----------|-------------------|
| **DISABLED** | Без RAG | Обычный чат без поиска в документах | ⚡ Самый быстрый |
| **STANDARD** | Быстрый поиск | Векторный поиск по семантическому сходству | ⚡⚡ Быстрый |
| **WITH_RERANKER** | Умный поиск | Векторный поиск + AI-переранжирование | ⚡⚡⚡ Средний |
| **WITH_MULTIQUERY** | Премиум поиск | Multi-query expansion + переранжирование | ⚡⚡⚡⚡ Медленный |

#### Возможности RAG

- 📄 Индексация документов с генерацией эмбеддингов (Ollama nomic-embed-text)
- 🔎 Семантический поиск по косинусному сходству
- 🧠 Переранжирование результатов AI моделью (Ollama llama3.2)
- 📊 Мульти-query расширение запросов для улучшения релевантности
- 💾 Хранение индексов в файловой системе

### 🛠️ Инструменты (Tools)

#### Встроенные инструменты (модуль `app`)

| Инструмент | Назначение |
|------------|-----------|
| **ContextWindowManager** | Управление размером контекста: автоматическая суммаризация при превышении лимита токенов (по умолчанию 80% от максимума), отслеживание использования контекстного окна |
| **TokenCompareManager** | Сравнение использования токенов между последовательными сообщениями, отображение дельты (увеличение/уменьшение) |
| **ModelInfoManager** | Отображение информации о модели, которая обработала запрос (например, GigaChat-Pro, DeepSeek-R1) |

#### MCP инструменты (AiCourseBackend)

| Инструмент | Возможности |
|------------|-------------|
| **AnimeTool** | Поиск аниме по названию или жанру через Jikan API. Возвращает: название, рейтинг, жанры, описание, URL на MyAnimeList |
| **NotesTool** | Управление списком задач:<br>• `add_notes_bulk` — добавить несколько заметок<br>• `get_notes` — получить заметки (фильтры: active/completed/all)<br>• `complete_notes_bulk` — отметить заметки как выполненные<br>Хранение: JSON файлы в `users_data/` |
| **AdbTool** | Управление Android устройством через ADB (10 инструментов):<br>• Список устройств, выбор активного устройства<br>• Открытие приложений по названию<br>• Умный поиск (находит поле поиска и вводит текст)<br>• Ввод текста, навигация (Back/Home)<br>• Открытие URL в браузере |

#### MCP инструменты (NotificationBackend)

| Инструмент | Возможности |
|------------|-------------|
| **send_telegram_message** | Отправка сообщений в Telegram через Bot API. Требуется настройка `TELEGRAM_BOT_TOKEN` и `TELEGRAM_CHAT_ID` |

### ⚙️ Настройки

- 🔌 Выбор API провайдера (GigaChat / HuggingFace)
- 📜 Стратегия работы с историей (PLAIN / ONE_MESSAGE / SUMMARIZE)
- 📊 Режим отслеживания токенов
- 🔧 Управление MCP клиентами (просмотр доступных инструментов)
- 🔍 Настройка RAG режима (переключение между 4 режимами)

---

## 🔌 MCP Сервисы

### AiCourseBackend

**Технологии:**
- Ktor Server 2.x
- MCP Kotlin SDK
- Docker (Alpine Linux + JRE 17)

#### Инструменты

<details>
<summary><b>AnimeTool</b> (<code>search_anime</code>)</summary>

**Описание:** Поиск аниме по названию или жанру через Jikan API

**Параметры:**
- `query` (string) — поисковый запрос

**Возвращает:**
Список аниме с информацией:
- Название (на английском и японском)
- Рейтинг (0-10)
- Жанры
- Краткое описание (synopsis)
- Ссылка на MyAnimeList

**Пример:**
```json
{
  "query": "attack on titan"
}
```

**API:** https://api.jikan.moe/v4/anime
</details>

<details>
<summary><b>NotesTool</b> (3 инструмента)</summary>

**1. `add_notes_bulk`**
- Параметры: `notes` (array of strings)
- Добавляет несколько заметок в список задач пользователя
- Возвращает: количество добавленных заметок

**2. `get_notes`**
- Параметры: `filter` (enum: "active" | "completed" | "all")
- Получает список заметок с фильтрацией по статусу
- Возвращает: markdown чек-лист (`[ ] Task` или `[x] Completed`)

**3. `complete_notes_bulk`**
- Параметры: `snippets` (array of keywords)
- Отмечает заметки как выполненные по ключевым словам (fuzzy match)
- Возвращает: количество отмеченных задач

**Хранение:** JSON файлы в директории `users_data/` (один файл на пользователя)

**Модель данных:**
```json
{
  "notes": [
    {
      "text": "Task description",
      "completed": false,
      "timestamp": "2025-01-13T12:00:00Z"
    }
  ],
  "fcmToken": "firebase_token"
}
```
</details>

<details>
<summary><b>AdbTool</b> (10 инструментов)</summary>

**Описание:** Управление Android устройствами через ADB (Android Debug Bridge)

**Инструменты:**

| Название | Параметры | Описание |
|----------|-----------|----------|
| `adb_get_status` | — | Показать активное устройство |
| `adb_list_devices` | — | Список подключенных устройств |
| `adb_select_device` | `serial`, `model_hint` | Выбрать целевое устройство |
| `device_open_app` | `app_name` | Запустить приложение по названию |
| `device_smart_search` | `query` | Найти поле поиска и ввести запрос |
| `device_type_text` | `text` | Ввести текст в активное поле |
| `device_go_back` | — | Нажать кнопку "Назад" |
| `device_go_home` | — | Нажать кнопку "Домой" |
| `device_open_url` | `url` | Открыть URL через Intent |

**UI Автоматизация:**
- Использует `uiautomator dump` для получения XML дерева UI
- Regex парсинг координат элементов: `[x1,y1][x2,y2]`
- Автоматический расчет центра элемента для tap-событий

**Переменная окружения:**
- `ADB_PATH` — путь к adb исполняемому файлу (по умолчанию `adb` из PATH)
</details>

#### Дополнительные возможности

- 🕐 **Автоматическая суммаризация заметок** — каждый час генерирует AI-резюме активных задач
- 📲 **Push-уведомления** — отправка резюме через Firebase Cloud Messaging
- 🔗 **Session Management** — управление SSE соединениями для множественных MCP клиентов

#### Endpoints

- `GET /sse` — SSE подключение для MCP протокола
- `POST /messages?sessionId={id}` — Обработка MCP сообщений
- `POST /register-push-token` — Регистрация FCM токена для push-уведомлений

### AiCourseNotificationBackend

**Технологии:**
- Ktor Server 2.x
- Telegram Bot API
- Docker (Alpine Linux + JRE 17)

#### Инструмент

**`send_telegram_message`**

**Параметры:**
- `message` (string) — текст сообщения

**Описание:** Отправка сообщений в Telegram через Bot API

**Требуется конфигурация:**
```env
TELEGRAM_BOT_TOKEN=your_bot_token
TELEGRAM_CHAT_ID=your_chat_id
```

**API Endpoint:** `https://api.telegram.org/bot{TOKEN}/sendMessage`

**Порт:** 8081

---

## 🔗 Дополнительные проекты

### mcp-bridge

**Назначение:** Мост между локальными MCP серверами и Claude/Cursor IDE

**Принцип работы:**
- Подключается к MCP серверу через SSE (Server-Sent Events)
- Получает endpoint для отправки сообщений
- Транслирует MCP протокол в stdin/stdout формат для Claude/Cursor
- Буферизует сообщения до получения endpoint

**Технологии:**
- Node.js (CommonJS)
- `eventsource` v4.1.0 (SSE клиент)

**Использование:**

```bash
cd mcp-bridge
npm install

# Отредактировать BASE_HOST в bridge.js
# BASE_HOST = 'http://127.0.0.1:8080'

node bridge.js
```

**Конфигурация:**
```javascript
// bridge.js
const BASE_HOST = 'http://127.0.0.1:8080';  // URL вашего MCP сервера
const SSE_PATH = '/sse';
```

### scripts

**Содержание:**

#### `start_ollama.ps1`

PowerShell скрипт для запуска Ollama с конфигурацией AI Course

**Функции:**
- Настройка переменных окружения:
  - `OLLAMA_MODELS` — путь к директории с моделями
  - `OLLAMA_HOST` — прослушивание на всех интерфейсах (0.0.0.0)
  - `OLLAMA_ORIGINS` — CORS: разрешить все источники
- Напоминание о необходимости загрузки моделей (`nomic-embed-text`, `llama3.2`)
- Запуск `ollama serve`

**Использование:**
```powershell
cd scripts
.\start_ollama.ps1
```

---

## 🗺️ Планы на развитие

### 🔥 Краткосрочные планы (высокий приоритет)

#### Рефакторинг модуля `app`
- [ ] Разделить `app` на более мелкие модули по функциональности
- [ ] Вынести UI компоненты в отдельный модуль `ui`
- [ ] Создать модуль `chat` для доменной логики чата
- [ ] Переместить инструменты в отдельный модуль `tools`

#### Улучшение системы инструментов
- [ ] **Заменить ChatStrategy на пайплайн из компонентов `coretools`**
- [ ] Реализовать цепочку: **User → LLM → (tool call) → ToolExecutor → (result) → LLM → final answer**
- [ ] Добавить поддержку множественных вызовов инструментов в одном запросе
- [ ] Интеграция с `ToolInterceptor` для логирования, валидации и метрик

#### Управление чатами
- [ ] Множественные чаты с UI для переключения между ними
- [ ] Сохранение индивидуальных настроек для каждого чата
- [ ] UI для управления списком чатов (создание, удаление, архивирование)
- [ ] Экспорт/импорт истории чата

---

### ⚡ Среднесрочные планы (средний приоритет)

#### Гибкая настройка инструментов
- [ ] UI для включения/отключения RAG для конкретного чата
- [ ] Добавление/удаление кастомных MCP соединений через UI
- [ ] Настройка вывода дополнительной информации (токены, модель, контекст)
- [ ] Управление суммаризацией контекста (порог, количество сохраняемых сообщений)
- [ ] Выбор модели для каждого чата индивидуально

#### UI адаптация под инструменты
- [ ] Динамическое изменение интерфейса чата в зависимости от активных инструментов
- [ ] Специализированные карточки для разных типов ответов (аниме, заметки, ADB команды)
- [ ] Визуализация работы RAG (показать найденные документы, релевантность)
- [ ] Анимации для tool execution progress

#### Улучшение RAG
- [ ] Поддержка множественных индексов в одном чате
- [ ] UI для управления индексами (создание, обновление, удаление)
- [ ] Предпросмотр найденных документов перед отправкой в LLM
- [ ] Настройка параметров поиска (top-k, similarity threshold, chunk size)
- [ ] Статистика по использованию RAG (hit rate, latency)

---

### 🚀 Долгосрочные планы (низкий приоритет)

#### Создание универсального артефакта
- [ ] Выделение core логики работы с LLM в отдельную библиотеку (JVM/Android)
- [ ] Документация API для переиспользования в других проектах
- [ ] Примеры интеграции в различные типы приложений
- [ ] Публикация в Maven Central как `aicourse-llm-toolkit`

#### Расширение MCP экосистемы
- [ ] Новые MCP инструменты:
  - Погода (Weather API)
  - Новости (News API)
  - Календарь (Google Calendar)
  - Файловая система (file operations)
- [ ] Поддержка сторонних MCP серверов
- [ ] Marketplace для MCP инструментов (UI для обнаружения и подключения)

#### AI функции
- [ ] Поддержка streaming ответов (Server-Sent Events)
- [ ] Multimodal вход (изображения, аудио) через Camera/Gallery
- [ ] Voice-to-text интеграция (распознавание речи)
- [ ] Экспорт истории чата (PDF, Markdown, HTML)
- [ ] Поиск по истории сообщений

#### DevOps
- [ ] CI/CD pipeline (GitHub Actions)
- [ ] Автоматические unit/integration тесты для всех модулей
- [ ] Kubernetes deployment для backend сервисов
- [ ] Мониторинг и логирование (Prometheus, Grafana)
- [ ] Performance benchmarks для RAG и tool execution

---

## 📄 Лицензия

MIT License (или другая по выбору автора)

---

## 🤝 Contributing

Вклад в проект приветствуется!

- **Issues:** Сообщайте о багах и предлагайте новые фичи через [GitHub Issues](https://github.com/your-username/AiCourse/issues)
- **Pull Requests:** Форкните репозиторий, создайте ветку с изменениями и отправьте PR

---

## 📞 Контакты

- **GitHub:** [your-username](https://github.com/your-username)
- **Email:** your-email@example.com

---

**Статус проекта:** 🚧 В активной разработке

> Проект создан как исследовательская платформа для экспериментов с LLM, RAG и MCP. Архитектура продолжает эволюционировать на основе новых практик и паттернов.
